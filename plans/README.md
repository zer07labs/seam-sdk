# Build plans

All `seam-sdk` build plans live here, **git-tracked** (unlike `seam-runtime`, where `plans/` is
gitignored scratch). Convention as of 2026-08-14: **active/pending plans sit in `plans/`;
delivered plans move to `plans/archive/`** with a dated delivery-verification note prepended —
verdicts are verified against code in this repo, `seam-runtime`, and `seam-adapters`, never
against a status table alone.

## Active / pending

| Plan | Status |
|---|---|
| `aitp-timestamp-encoding.md` | **BLOCKED — planned, not started.** Issue [#105](https://github.com/zer07labs/seam-sdk/issues/105): the pinned-key admission-proof preimage's timestamp slot moves from an 8-byte big-endian i64 to the UTF-8 bytes of its base-10 decimal string (RFC-AITP-0002 §3.1 erratum). Severity is **interop, not outage** — our fleet is internally consistent today; what fails is third-party AITP admission into Seam and our proofs against the reference `aitp-verifier-py`. Nine phases across all five languages; the domain separator is unchanged, so the two forms are distinguishable only by trying both. **Blocked upstream, re-confirmed 2026-09-13:** [seam-runtime#684](https://github.com/zer07labs/seam-runtime/issues/684) is open with no comments and the runtime still pins `aitp-handshake` `0.4.1`, so the dual-accepting verifier is not in `seamd:main` — and seam-sdk's `integration` job pulls that image, which makes the PR literally unmergeable before then (Blocker A). **This plan was untracked until 2026-09-13** — written 2026-09-05 and left in a working tree, so it was invisible to every session but one. Its ten mint-site line references were re-verified verbatim at that point; its Phase 7/8 acceptance numbers had rotted and were corrected, one of them into a criterion that could no longer be satisfied at all (see the refresh header). |
| `build-agent-ingress.md` | **PENDING** — narrowed 2026-08-24. The §A core scenes (Suspended→raise→resume, denied admission) and §C `StepUsage` wiring shipped in `seam-adapters` PR #42; what remains is the **§B MCP server** (seam-sdk [#40](https://github.com/zer07labs/seam-sdk/issues/40), deliberately unbuilt — no named customer) and the **§D public-access DoD**. See the refresh header in the plan. |
| `consolidation-2026-08-14.md` | **RECORD + BACKLOG** — the 2026-08-14 full-repo review (plan triage, 6-track code review of seam-sdk + the runtime contract boundary), the fixes applied, and the residual backlog. Backlog swept 2026-08-24: four of five entries retired in place with citations; the survivors are §B/§D of `build-agent-ingress.md`. |

## Cross-repo asks

[`cross-repo/`](cross-repo/) holds plans that target **other** repos — currently seven asks against
`seam-runtime` ([#418–#423](https://github.com/zer07labs/seam-runtime/issues/418) and
[#525](https://github.com/zer07labs/seam-runtime/issues/525)) and one against `seam`
([#26](https://github.com/zer07labs/seam/issues/26)). They live here
because writing into a sibling repo needs a gate and reading one never does: the plan stays where it
was written, and the target repo fetches it. Each is paired with a tracking issue there. See
[`cross-repo/README.md`](cross-repo/README.md) for the index and what blocks what.

## Archived (delivered, verified)

| Plan | Delivered |
|---|---|
| `archive/post-adoption-hardening-and-acdp-readiness.md` | 10 phases across PRs #79–#84, pressure-tested by two Opus review rounds **before any code** (see its `## Plan review`). Closes the fail-open residue the last contract adoption left (`SessionStep.collective_outcome` was generated but unreachable safely — now `python/seam_sdk/_collective.py`, fail-closed), shuts the publish-time gencode/floor skew that let `v0.7.43` ship metadata it did not honour (`publish.yml:318-328`), and adds a **field-level** contract manifest so the next additive field cannot regenerate in unwired — the verb-level failure of [#49](https://github.com/zer07labs/seam-sdk/issues/49), one level down. Carries [#50](https://github.com/zer07labs/seam-sdk/issues/50), [#52](https://github.com/zer07labs/seam-sdk/issues/52), [#48](https://github.com/zer07labs/seam-sdk/issues/48), [#73](https://github.com/zer07labs/seam-sdk/issues/73), [#76](https://github.com/zer07labs/seam-sdk/issues/76), all verified CLOSED. Phase 9 (ACDP P1a) adopted 2026-08-31. Archived 2026-09-13 — it had been DONE and sitting in the active index since 2026-08-31, the longest-standing of the five archived in that pass. |
| `archive/gate-blindness-hardening.md` | The plan this repo's whole vacuity discipline comes from: **a gate whose result is decided by something other than the property it names.** Cited by nearly every guard test added since, including ones written after it was delivered. Archived 2026-09-13 with delivery verified against this tree, and it turned up the only finding of the five: **Phase 5's artifact (`contract/expected-local-lag.txt`) is gone, but its machinery in `check-contract.sh` survives and is correctly dormant** — the downgrade arm is file-guarded, so an absent file means no downgrade and a refusal stands. That is the fail-safe direction, and a re-armable facility is the right residue for a phase about a *temporary* gap. Recorded so nobody mistakes live-but-dormant code for dead code, or re-files the deletion as a regression. |
| `archive/digest-correctness-and-gate-repair.md` | 8 phases, 3 PRs, out of a three-way audit: the guards largely held under mutation, but the shipped crypto carried four demonstrated cross-language divergences, and `contract/wire-framing.json`'s adoption latch had sat `false` since the runtime adopted the handshake — so the one gate that exists to prevent another 0.7.17 warned and tagged instead of refusing. Also closed the unwatched `seam.event.v1` verb surface and landed the ACDP P3 readiness tripwire ahead of an adoption that could not happen until the runtime merged P3; Phase 8 adopted it 2026-09-04. Archived 2026-09-13 with the delivery verification this plan's own row recorded as missing: the latch and BOTH halves of its staleness repair, the `2^53` guards in both languages, the four divergences recorded with their failing inputs at `COMPATIBILITY.md:466-471`, the event-surface preconditions wired into the real run, and ACDP P3's strip semantics in the shipped crypto. One audit finding was **refuted** rather than actioned — see the plan's Context §3. |
| `archive/consumer-decoders-and-event-surface.md` | Issues [#87](https://github.com/zer07labs/seam-sdk/issues/87), [#88](https://github.com/zer07labs/seam-sdk/issues/88), [#85](https://github.com/zer07labs/seam-sdk/issues/85), five phases in three PRs (#90, #93, #94). The decoder half is `policy_enforcement_of` / `policyEnforcementOf`, the presence-aware pair that stops an absent `policy_enforcement` reading as "no policy was enforced"; the contract half is `contract/event-field-manifest.txt`, closing the last unmanifested surface. Archived 2026-09-13 with delivery verified against this tree — the manifest now carries **94** fields rather than the 90 it shipped, which is the manifest doing its job (the growth arrived through #117, named rather than silent). This row said "shipped (#92)" while in progress; there is no PR #92, and the correction is kept rather than erased. |
| `archive/registry-drift-check.md` | The STATE half of issue [#100](https://github.com/zer07labs/seam-sdk/issues/100): `publish.yml`'s notifier covers the EVENT (a publish that ran and refused); this covers the registry lagging the source, which is what stays true across re-runs, re-tags, and a release that was never dispatched at all — the case nothing could see, because nothing executed. Eight phases in three PRs; Phase 8 **dropped** as scope creep (the real fix was `--atomic`, filed separately and since exercised for real on the v0.14.2 re-tag). 60+ mutations across the reporting phases, and BOTH verification rounds found defects in work already called proved — Phase 6's round found its headline fix had MOVED the defect rather than closed it, and Phase 7's found four mutations its battery had recorded as caught were survivors. Archived 2026-09-13 with its delivery verified against this tree: script, workflow, the `17 */2 * * *` schedule, the four-entry canary roster, the `deliberately-unpublished` suppression, the `status=completed` heartbeat, and the never-closes-an-issue property all checked rather than assumed. |
| `archive/record-digest-v3.md` | `record_digest_v3` in Python, TypeScript and `verify/` — all three transcribed clean-room from the published spec, agreeing with seam-runtime's fourth implementation on every committed vector. All phases delivered including the 6a/6b streamed arms (`admin.py`/`admin.ts` handle v2 and v3 and refuse anything newer) and the Phase 8 tag-13 divergence. Issue [#56](https://github.com/zer07labs/seam-sdk/issues/56), closed 2026-08-25; archived 2026-08-31 with its delivery verified against this tree. *(PRs #58, #63.)* |
| `archive/authorize-single-canonicalization.md` | Closed the double-canonicalization class in `Authorize`: canonicalization failures are typed `SeamError`s, the SDK's own retry path stops re-deriving, the int and float arms agree by construction, and a caller may supply the canonical bytes so exactly one derivation exists. Issue [#60](https://github.com/zer07labs/seam-sdk/issues/60), closed 2026-08-25. **Delivered 2026-08-25 but never indexed here at all** — the omission was found and fixed on 2026-08-31. *(PR #68.)* |
| `archive/close-out-w1-w7-loose-ends.md` | The loose ends #51 left: `verify/` now **compiled** at its declared MSRV in CI (not merely asserted), the 2026-08-14 residual backlog swept and retired in place with citations, the CrewAI/protobuf finding recorded in `COMPATIBILITY.md` + `DECISIONS.md` and re-derived weekly by a live resolution probe, and the six `seam-runtime` asks given a version-controlled home in `cross-repo/`. *(PR #53.)* |
| `archive/sdk-exec-w1-w7.md` | The W1–W7 exec workstream: one batched contract regeneration (additive, proven under `buf breaking --config FILE`), the release-exposure gates (CI-green gating, npm install gate, registry smoke, cross-language framing coverage, the wire-framing handshake), `COMPATIBILITY.md`, and the digest dual-verify obligation. Cross-repo asks filed as seam-runtime #418–#423 rather than delivered here. *(PR #51.)* |
| `archive/build-sdk-session-budget.md` | Py + TS incremental session lifecycle + the enterprise-6.2 budget surface (suspend→raise→resume), live-verified against a real `seam-grpc`. Resume later moved to the management plane (rt-D); data-plane `resume_session` is a documented tombstone. *(PR #4.)* |
| `archive/build-sdk-hardening-p110-h3-h4.md` | Typed issuer-mismatch error (P1.10), `SeamAdminClient` with admin/erasure wrappers (H3), `features` on `run_decision` (H4), data-plane parity (H5), `SeamEvents` streaming + the typed-error taxonomy. Bearer model superseded by operator tokens (runtime #175 / SDK PR #16). *(PRs #5/#7/#8, versions 0.1.0→0.3.0.)* |
| `archive/adopt-runtime-2026-07.md` | SDK adoption of the runtime backlog-closeout landing: `verify_party_attestation` wrappers, `verify/` upgraded INTEGRITY→AUTHENTICITY (`chain --issuer`: attestation + digest-v2 recompute), conformance KATs, differential-harness parity. Phase 0 landed runtime-side as the `seam.event.v1` extraction. *(Merged; the #175 bearer residual closed by PR #16.)* |
