# Compatibility, support window, and what "independently verifiable" actually covers

**Read this before pinning a version.** This SDK's version number cannot tell you what you need to
know, and the reason is structural rather than an oversight. That is the first section, because
everything else here exists to compensate for it.

Every row and claim below cites a `file:line` that resolved when it was written. A row without one
is a row that gets deleted, not softened — two verified rows are worth more than a full grid that is
wrong.

---

## 1. The lockstep rule, and the consequence you have to plan around

`seam-sdk` does not choose its own version. The `seam-runtime` release is the version authority: it
fires a dispatch, `release-on-runtime.yml` stamps `ts/package.json` and `python/pyproject.toml` to
match, and tags. One version everywhere.

The consequence is quoted verbatim from `CHANGELOG.md:9-12`, unsoftened:

> One consequence is worth knowing when reading these entries: **this SDK cannot express its own
> semver.** A breaking change here ships under whatever number the runtime's history computes, which
> may be a patch. Where that happens it is called out at the top of the version's section rather than
> left for a consumer to discover.

**So a version range cannot protect you from a break.** `>=0.7.20,<0.8` does not mean "no breaking
changes" the way it would for a normally-versioned package; it means "the runtime released these,
and one of them may have moved the wire".

What to do instead, in descending order of usefulness:

1. **Pin exactly** and upgrade deliberately, reading `CHANGELOG.md` for the range you cross.
2. **Run the conformance vectors after upgrading** — `conformance/vectors.json` is generated by
   executing the runtime's reference implementation, so agreement with it is agreement with the
   runtime, not with our own tests.
3. **Watch the known-bad bands** in §3. They are permanent and nothing was yanked.

---

## 2. Verified compatibility rows

| Consumer | Constraint on `seam-sdk` | Verified at |
|---|---|---|
| `seam-adapters` (`seam-agent-core[sdk]`) | `seam-sdk>=0.7.20,<0.8` | `seam-adapters/core/pyproject.toml:22` |
| `seam-aegis` | `seam-agent-core[sdk]>=0.1,<0.2` (reaches this SDK transitively) | `seam-aegis/pyproject.toml:28` |

**One caveat on the first row, because the lockfile disagrees with the constraint and both are
true.** `seam-adapters/uv.lock:3921` resolves `seam-sdk` **0.7.9** — below the declared floor — and
that is not a violation: `seam-adapters/pyproject.toml:32` overrides the dependency with an
unconditional editable path source (`{ path = "../seam-sdk/python", editable = true }`), so the lock
records the sibling checkout rather than a resolved release. A reader comparing the two numbers
should not conclude the floor is being ignored.

### Runtime ↔ SDK

There is exactly one rule and it needs no table: **the SDK version equals the runtime version.**
`ci.yml`'s `version-lockstep` job and `publish.yml`'s `version-check` job both enforce it, so a
mismatch cannot ship.

---

## 3. Known-bad versions — permanent, and this document is the only barrier

**Nothing was yanked.** That decision is recorded at `CHANGELOG.md:227-231` and is not being
re-litigated here: the affected versions install and produce a clear auth error rather than
corrupting data, and revoking installability under a floor already in wide use has a larger blast
radius than a loud advisory. The consequence is that **these versions remain installable**, so this
table is the mitigation.

| Range | Symptom | Root cause |
|---|---|---|
| **0.7.13 – 0.7.15** | `import seam_sdk` fails: `ModuleNotFoundError: No module named 'seam'` | `publish.yml` ran raw `buf generate` instead of `make generate`, so the published wheel was never importable. The publish guard checked file *presence*, not import, and so could not fail for the reason it was named for. |
| **0.7.16 – 0.7.19** | Every `authorize()` fails `UNAUTHENTICATED: admission ticket is not valid` — **the ticket is fine** | `seam-runtime` #286 moved the per-call proof-of-possession signature from v1 to v2. Every SDK published before the fix still signed v1. The framing had no conformance vector, and both SDKs' tests verified a signature against a payload the test itself rebuilt — so a self-consistent wrong answer stayed green. |

**Floor: 0.7.20.** The first release that is both importable and wire-correct.

Neither defect is visible from the version number. That is the lockstep rule in §1 doing exactly what
it says it does.

---

## 4. Per-language support reality

"Published to Cloudsmith" is true for **two of five** languages. A consumer must not have to learn
this by trying.

| Language | How you get it | Versioned? | Status |
|---|---|---|---|
| **Python** (`seam-sdk`) | Cloudsmith `zer07labs/internal` (`.github/workflows/publish.yml:255`) | Yes | **Published + supported** |
| **TypeScript** (`@zer07labs/seam-sdk`) | Cloudsmith `zer07labs/internal` (`.github/workflows/publish.yml:130`, `ts/package.json:12`) | Yes | **Published + supported** |
| **Go** | Module proxy, from the `go/vX.Y.Z` tag (`.github/workflows/release-on-runtime.yml:120`) | Tag only — no in-tree version | Resolvable, crypto shim only |
| **Java** | Build from source | **No `version`, no `maven-publish`** | Build-from-source only |
| **Kotlin** | Build from source | **No `version`, no `maven-publish`** | Build-from-source only |

**Go, Java and Kotlin are crypto shims, not clients** (`README.md:124`). They carry no generated
transport and no verb methods — they implement AID derivation, the pinned-key presentation, TCT
verification and the commitment digest, and nothing else. If you need to *call* Seam, that is Python
or TypeScript.

The practical consequence: **a new RPC verb costs the three shims nothing**, and a change to a crypto
**framing** costs them everything. That is the axis 0.7.17 broke on.

### Dependency floors

| Package | Floor | Why |
|---|---|---|
| Python: `protobuf` | `>=7.36.0,<8` | **Derived, not chosen** — must be ≥ the gencode compiled into `seam_sdk/_gen`. `python/tests/test_protobuf_floor.py` derives it from the emitted stubs and fails when a regeneration outruns it. |
| Python: `grpcio` | `>=1.64` | Derived the same way — needs both halves of the registered-method calling convention. |
| Python | `>=3.10` | The gencode's own floor. |
| Rust (`verify/`) | **MSRV 1.85** | Derived from the resolved dependency graph (`prost` 0.14.4, `base64ct` 1.8.3, `zeroize` 1.9.0); `verify/tests/msrv.rs` asserts it. |

**A protobuf floor bump is a real upgrade cost even when the contract change is additive.** The
`>=7.35.1 → >=7.36.0` move is an example: the wire change that triggered it was additive
(`buf breaking` clean under `FILE`), but a consumer pinned at `protobuf==7.35.1` fails to resolve,
and one who force-installs gets a `VersionError` at `import seam_sdk`, not a wire error. "Additive on
the wire" and "no consumer impact" are different claims.

---

## 5. What "independently verifiable" does and does not cover

The published verifier (`verify/`) links **zero** Seam crates, and that is a CI gate rather than a
comment — `.github/workflows/ci.yml:289-297` runs `cargo tree -e normal` and fails on any `seam-*`. What it verifies:

**Covered:**

- **Chain integrity** — the `seam-event.v1` hash chain, from the stream alone.
- **Authenticity** — every `CHAIN_HEAD_ATTESTATION` verifies against a pinned issuer key and sits at
  the head it attests; every v2 `DECISION_SEALED` digest is recomputed from its payload.
- **Erasure certificates** — from the issuer AID alone, no Seam credential and no network call.

**NOT covered — stated plainly, because the phrase "independently verifiable" would otherwise carry
implications it does not support:**

- **Truncation. The verifier cannot detect it.** A stream cut at the tail is internally consistent
  and verifies **green**. Detecting truncation requires a third-party-observable append-only feed,
  and none is published: `checkpoint` and `transparency` have zero hits across the runtime's crates,
  and chain anchors exist only as outbox events with no route serving them. So an auditor can prove
  the chain they were **given** is consistent, and **cannot prove it is the whole chain.** Tracked at
  [`seam-runtime#422`](https://github.com/zer07labs/seam-runtime/issues/422); this claim changes only
  when that lands.
- **The commitment digest.** `verify/` does not implement `seam-commitment-digest:v1` at all — the
  string appears nowhere in it. The five crypto shims implement it; the published verifier does not.
  `python/tests/test_framing_rationale_is_documented.py` guards against a doc claiming otherwise.
- **Proof acquisition.** An external auditor cannot fetch a proof: both proof verbs require an
  enrolled, tenant-matched subject. The intended answer is an exported evidence bundle rather than a
  cross-tenant read grant — tracked at
  [`seam-runtime#421`](https://github.com/zer07labs/seam-runtime/issues/421).

### On independence, honestly

Two verifier implementations exist and a differential harness drives both over the same streams. The
five SDK crypto shims agree byte-for-byte. But **they are ports by one author from one source, not
independent implementations**, and a third verifier by the same author would add nothing.

Real independence means someone working from **spec text alone**. The record digest has that
(`seam-runtime/docs/specs/seam-event.v1.md:372,379,399`, byte-exact for v2 and historical v1). The
commitment digest does not — its normative description is a doc comment in a private repo, tracked at
[`seam-runtime#423`](https://github.com/zer07labs/seam-runtime/issues/423).

---

## 6. Support window

**N-2 minors** — the current runtime minor and the two before it.

With the caveat that makes it meaningful: **"minor" is the runtime's**, not this SDK's, because this
SDK has no semver of its own (§1). In practice, treat the **0.7.20 floor** as binding regardless of
the window; it is older than N-2 and still the correct floor.

There was no declared window before this document, so this is a starting commitment rather than a
long-standing one, and it is stated as such.

---

## 7. A cross-repo coupling worth knowing about

Merging to `main` in this repo takes effect in **`seam-runtime`'s CI immediately**, with no version
gate in between. Two of its jobs — `sdk-digest-parity` and `differential-parity` — check this
repository out at its **default branch, unpinned** (`seam-runtime/.github/workflows/ci.yml`, neither
checkout specifies a `ref:`).

Concretely:

- **`conformance/vectors.json` is effectively owned by the runtime's emitter.** The parity job
  byte-diffs the *whole file* against `cargo run -p seam-client --example conformance_vectors`. A
  block added here by hand — even a correct one — turns `seam-runtime`'s CI red. **New vectors must
  originate in the runtime.**
- **`python/seam_sdk/crypto.py`'s digest functions are resolved by exact name** from that job. A
  rename here breaks merges there.

This is a real constraint on what a change in this repo can do unilaterally, and it is documented
here because nothing else in the repo says so.

---

## Changing this document

Every row must cite a `file:line` that resolves. If a claim cannot be verified, **delete it** — a
compatibility matrix with aspirational rows is worse than a short one, because a reader cannot tell
which rows were checked.
